[package]
name = "kenzie-dashboard"
version = "0.1.0"
edition = "2021"

[lib]
name = "kenzie_dashboard"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
